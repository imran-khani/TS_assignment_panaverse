const authorName: string = "Albert Einstein";
const quote: string = `“Two things are infinite: the universe and human stupidity; and I'm not sure about the universe.”`;

console.log(`${authorName} once said, ${quote}`);
